[package]
name = "simple-cube"
version = "0.1.0"
edition = "2024"
description = "Super simple cube example using fastn's simplified API"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
fastn = { path = "../../fastn" }
